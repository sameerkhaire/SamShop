import { DirAppDirective } from './dir-app.directive';

describe('DirAppDirective', () => {
  it('should create an instance', () => {
    const directive = new DirAppDirective();
    expect(directive).toBeTruthy();
  });
});
